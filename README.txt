Instructions!!

This app is developed using react and redux with webpack and babel.
For that, a simple server that responses with the statics files is needed.

1- npm install
2- npm run server
3- visit http://localhost:3000

